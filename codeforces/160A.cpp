#include <bits/stdc++.h>
using namespace std;


void solve() {
    int n;cin>>n;

    //taking all the coin values as input
    vector<int> c;
    while(n--){
        int c;cin>>c;
        c.push_back(c);
    }

    //logic to find the min no of coins to
    //be strictly greater than other twin

    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
}