# Trapping Rainwater

- 2 Approaches

- 1st Approach

prefix sum is used.

first we calculate the prefixMax and the suffixMax for all the elements

then take min of both and subtract that from the current height of the building

- Note
When building a vector with push_back() while traversing backwards, its indices do NOT yet match the original array indices. Use .back() for the value you just calculated.

- 2nd Approach (optimized)

we will use only a vector to store the values for the suffixMax and then we can calculate the suffixMax
while iterating over the loop.


