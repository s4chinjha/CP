# Trapping Rainwater

- 2 Approaches

- 1st Approach

prefix sum is used.

first we calculate the prefixMax and the suffixMax for all the elements

then take min of both and subtract that from the current height of the building

- Note
When building a vector with push_back() while traversing backwards, its indices do NOT yet match the original array indices. Use .back() for the value you just calculated.

one another Approach(to try by ourselves)

we will use only a vector to store the values for the suffixMax and then we can calculate the suffixMax
while iterating over the loop.

- 2nd Approach (optimized)
Observation:

we always work on the smaller one.

in this what we are going to do is.
first we take two variables leftMax and rigthMax.
and a total which stores the waterLogged

and two pointers to iterate over the whole list.
one for i = 0 and other for i = n-1

Working:

after this,
we start iterating on the loop and then
 let's say the elements are:
0 1 0 2 1 0 1 3 2 1 2 1

l=a[0]=0 r=a[n-1] = 1

we take the smaller and move save the max out of the a[i] and leftMax already stored.it in the variable leftMax if it is the left one and v.v.

in the total we store in total = min[leftMax,rigthMax] - a[i]
