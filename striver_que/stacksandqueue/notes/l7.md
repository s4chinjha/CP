# Nearest Smallest Element

we have to put the smallest to the left of the currentVal in the array or -1 if there is none.
and return For every element in the array


brute:
we iterate over the array i = 0 -> n-1
  we iterate i-1 -> 0
    if(the current element has a smaller on the right ){
      add that to the pse[nums[i]]
    }
    else{
      add -1 to the pse[]
    }
return pse[]


optimized:
same as the previous problem 
in this we are going from the left instead of right
