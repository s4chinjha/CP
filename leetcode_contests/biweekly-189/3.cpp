#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int kthDigit(long long k) {
        
        int b = 0;
        int num = 1;
        string s= "";

        long long bg = bg*10;

        if( b%2==0){

            while( (10*b) <=num+9){
                s + to_string(num);
            }
            b++;   

        }
        else{

        }

    }
};